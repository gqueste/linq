var React = require('react');

var Game = React.createClass({
    render: function() {
        return (
            /*jshint ignore:start */
            <div>

            </div>
            /*jshint ignore:end */
        );
    }
});

export default Game;
